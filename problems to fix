
Add @@index declarations to the Booking, Appointment, and Notification models in schema.prisma for their foreign key columns.


Add requireAuth middleware to all resource creation routes that require a logged-in user — clients, staff, bookings, slots, waitlist. Show each route file change.

Fix the JWT payload in /users/login (userController.js) to include the role field, matching the payload structure from /auth/login.

Add skip/take pagination to GET /appointments, GET /users, GET /clients, GET /bookings. Show the pattern once with appointmentController.js as the example.

Fix all POST create handlers to return 201 Created instead of 200. List every affected controller and show the one-line change pattern.

Add parseInt to the id parameter read in waitlistController.js getWaitlistById, updateWaitlistEntry, and deleteWaitlistEntry handlers.

Move the inline route handler business logic out of notificationRoutes.js into a proper controller function. Show the refactored controller and updated route.

Fix the typo in paymentRoutes.js where requireRole("STYLIST") should be requireRole("STAFF"). Show the corrected lines.

Add requireAuth and requireRole("ADMIN") to GET /reports/revenue and GET /reports/top-services in reportRoutes.js.

Show how to add afterEach cleanup in auth.test.js and booking.test.js so test data is deleted and tests can run repeatedly without failing.



Add @@index declarations to the Booking, Appointment, and Notification models in schema.prisma for their foreign key columns.

Remove the @default(CASH) from Payment.provider in schema.prisma and add application-level validation that the provider is always explicitly set.

Show how to remove the Joi dependency from package.json and confirm no validator files actually use it.

Show the pattern to standardise all model files to use named exports consistently.

Rewrite the top-services query in reportController.js to use Prisma _count aggregation instead of loading all appointment records.

Rewrite the top-services query in reportController.js to use Prisma _count aggregation instead of loading all appointment records.

Show a clean, consistently formatted version of staffController.js.
