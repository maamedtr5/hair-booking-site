diff a/src/controllers/slotController.js b/src/controllers/slotController.js	(rejected hunks)
@@ -41,7 +41,7 @@ export const getSlots = async (req, res) => {
   try {
     // Parse query params, default to skip=0, take=10
     const skip = parseInt(req.query.skip) || 0;
-    const take = parseInt(req.query.take) || 10;
+    const take = parseInt(req.query.take) || 500;
 
     const slots = await prisma.slot.findMany({
       skip,
