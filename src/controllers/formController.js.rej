diff a/src/controllers/formController.js b/src/controllers/formController.js	(rejected hunks)
@@ -28,7 +28,7 @@ export const getForms = async (req, res) => {
   try {
     // Parse query params, default to skip=0, take=10
     const skip = parseInt(req.query.skip) || 0;
-    const take = parseInt(req.query.take) || 10;
+    const take = parseInt(req.query.take) || 500;
 
     const forms = await prisma.form.findMany({
       skip,
